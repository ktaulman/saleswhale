const express=require('express');
const app= express();
const bodyParser=require('body-parser');
const port=process.env.PORT||4000;
const cors=require('cors');

app.get('/herocontent',(req,res)=>{
    
})

app.listen()